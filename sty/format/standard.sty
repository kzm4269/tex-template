% figure
\renewcommand{\figurename}{Fig.}
\newcommand{\reffig}[1]{\figurename\ref{fig:#1}}

% table
\renewcommand{\tablename}{Table}
\newcommand{\reftab}[1]{\tablename~\ref{tab:#1}}

% equation
\newcommand{\refequ}[1]{式~(\ref{equ:#1})}
\makeatletter
	\renewcommand{\theequation}{\thesection.\arabic{equation}}
	\@addtoreset{equation}{section}
\makeatother

% theorem, definition, lemma
\newtheorem{theo}{定理}[section]
\newtheorem{defi}{定義}[section]
\newtheorem{lemm}{補題}[section]

% list
\renewcommand{\lstlistingname}{List}
\newcommand{\reflst}[1]{\lstlistingname~\ref{lst:#1}}
\lstset{
  language={C},
  basicstyle={\scriptsize\ttfamily},
  identifierstyle={\scriptsize\ttfamily},
  commentstyle={\scriptsize\ttfamily},
  keywordstyle={\scriptsize\bfseries},
  ndkeywordstyle={\scriptsize},
  stringstyle={\scriptsize\ttfamily},
  numberstyle={\scriptsize},
  frame={tb},
  breaklines=true,
  columns=[l]{fullflexible},
  numbers=left,
  xrightmargin=0zw,
  xleftmargin=3zw,
  stepnumber=1,
  numbersep=1zw,
  lineskip=-0.5ex,
  tabsize=2,
  keepspaces=true,
  showstringspaces=false,
}
